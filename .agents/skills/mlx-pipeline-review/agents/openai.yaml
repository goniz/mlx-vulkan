interface:
  display_name: "MLX Vulkan Pipeline Review"
  short_description: "Review Vulkan code for async pipeline regressions"
  default_prompt: "Use $mlx-pipeline-review to review this Vulkan target for async pipeline and inference performance regressions."
