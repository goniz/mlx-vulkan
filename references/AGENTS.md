Use the submodules under this path as reference only, NEVER modify files here.

When you need to learn about a specific topic, use the `@explore` subagent to search through the relevant reference submodules for detailed information and examples.
